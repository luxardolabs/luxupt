# luxaudit — fleet dependency-vulnerability guard. Source auto-detected (poetry.lock).
# [[ignore]] is the ratchet dial: a deferred vuln is STILL reported, never hidden.
# Every entry needs a written reason; drain to zero.
#
# No deferrals: the ecdsa (LUXUPT-66) and cryptography-PKCS7 (CVE-2026-69247) findings
# were both transitive via the unmaintained python-jose. Replacing it with PyJWT
# (HS256 uses stdlib hmac) dropped both dependencies at the root — audit is clean.
